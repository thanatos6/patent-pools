server:
  port: 8080
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.suixingpay.bean

spring:
  http:
    multipart:
      max-file-size: 20MB
      max-request-size: 30MB
  profiles:
    active: dev
  datasource:
    platform: mysql
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: root