server:
  port: 8080
mybatis:
  mapper-locations: classpath:/com/suixingpay/mapper/*.xml
  type-aliases-package: com.suixingpay.pojo
spring:
  profiles:
    active: dev
  datasource:
    platform: mysql
    url: jdbc:mysql://localhost:3306/patent?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: 1933333333